[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[context.production.environment]
  NODE_VERSION = "18"
